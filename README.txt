Also please check and update the file paths before running as this uses
Google Drive to load the data in

First need to do pip install -r requirements.txt 
or if running in cell !pip install -r requirements.txt

Then separately need to do a pip install for mamba-ssm
!pip install mamba-ssm --no-build-isolation 